# SARIF自动修复Agent依赖包

# 核心依赖
PyYAML>=6.0
requests>=2.28.0
sarif-sdk>=0.5.0

# 可选依赖（用于增强功能）
# jsonschema>=4.0.0  # 用于验证SARIF格式
# rich>=12.0.0      # 用于美化终端输出
# click>=8.0.0      # 用于创建更优雅的命令行接口

# 开发依赖
# pytest>=7.0.0
# black>=22.0.0
# flake8>=5.0.0
